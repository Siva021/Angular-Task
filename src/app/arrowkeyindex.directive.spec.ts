import { ArrowkeyindexDirective } from './arrowkeyindex.directive';

describe('ArrowkeyindexDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowkeyindexDirective();
    expect(directive).toBeTruthy();
  });
});
